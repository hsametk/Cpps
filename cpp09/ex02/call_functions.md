main
 └─ PmergeMe::run(argc, argv)
      ├─ PmergeMe::parseArgs(argc, argv)
      ├─ gettimeofday(&t0, NULL)
      ├─ PmergeMe::vectorSort(vecInput)
      │    ├─ PmergeMe::vectorRecSort(vals, ids)
      │    │    ├─ PmergeMe::vectorRecSort(larges, largeIds)   (rekürsif)
      │    │    ├─ PmergeMe::jacobsthalOrderVec(pairCount)
      │    │    ├─ PmergeMe::vecIndexOfId(chainIds, largeIdentity)
      │    │    ├─ PmergeMe::vecBinaryInsertPos(chainVals, value, bound)
      │    │    └─ PmergeMe::vecBinaryInsertPos(chainVals, stragVal, chainVals.size())
      │    └─ (döndükten sonra sorted vector döner)
      ├─ gettimeofday(&t1, NULL)
      ├─ gettimeofday(&t0, NULL)
      ├─ PmergeMe::dequeSort(deqInput)
      │    ├─ PmergeMe::dequeRecSort(vals, ids)
      │    │    ├─ PmergeMe::dequeRecSort(larges, largeIds)   (rekürsif)
      │    │    ├─ PmergeMe::jacobsthalOrderDeq(pairCount)
      │    │    ├─ PmergeMe::deqIndexOfId(chainIds, largeIdentity)
      │    │    ├─ PmergeMe::deqBinaryInsertPos(chainVals, value, bound)
      │    │    └─ PmergeMe::deqBinaryInsertPos(chainVals, stragVal, chainVals.size())
      │    └─ (döndükten sonra sorted deque döner)
      ├─ gettimeofday(&t1, NULL)
      └─ ekrana sonuç yazdırma